# Task: Plan round 2 of Numberball Gameday's animation layer — verify the reconciliation refactor, fix testing-surfaced gaps, and design several new mechanics

You are planning an implementation for Sonnet to execute. **Do not write the implementation.** You *may* write small throwaway probe scripts purely to verify a load-bearing factual claim before it goes into the plan (see "Open factual questions" — several of these are genuinely unresolved from static reading and worth a probe rather than a guess). Produce a detailed, staged implementation plan (file-by-file, function-by-function where it matters) that Sonnet can follow directly, in the same style as `gameday-reconciliation-implementation-plan.md`. Call out open questions or risks rather than silently picking an answer.

## Goal, in priority order (unchanged from the last round — restate because it still governs everything below)

a) **Correctness is non-negotiable.** The rendered play must always resolve to exactly what the predetermined MLN result says happened. No design here should trade this away for realism, ever.

b) **Physical realism is the presentation objective.** Real distances, real (or rated) speeds, physically plausible reaction times.

c) **Player-attribute uniqueness, wherever the data supports it.**

## Project context

Numberball is a Streamlit + Supabase scouting app (repo: `C:\Users\Alex\PycharmProjects\numberball`). It ships a static-site game-replay viewer under `docs/` ("MLN Gameday") that animates individual plate appearances on an SVG baseball diamond, driven by `docs/js/app.js` (the animation engine) and `docs/js/trajectory.js` (ball-flight-only physics, out of scope — do not touch).

**This is round 2.** Round 1 (`gameday-reconciliation-fable-prompt.md`, planned by you; `gameday-reconciliation-implementation-plan.md`, the plan Sonnet executed) replaced a pile of hand-tuned, backward-solved timing constants with one shared physical-motion primitive, a per-player speed/pace model, and a single reconciliation mechanism that forward-simulates every contested event and closes any gap against the known verdict through a bounded, named knob set. That refactor **shipped and was verified against the plan** (see "Verified facts" below). Alex then play-tested the result and has a list of concrete feedback plus several requests for genuinely new mechanics that didn't exist before either round. This prompt is that list, pre-investigated against the current code so you're not starting from a guess.

**One more concurrent change to be aware of, separate from both rounds above:** a different Claude session, working on this same codebase in parallel, has already landed a real Statcast-fit correction to runner and outfield-pursuit acceleration constants (fact 26 below) — currently uncommitted in the working tree (`git diff docs/js/app.js` shows it directly, 45 insertions/9 deletions). Treat it as already-landed ground truth for planning purposes; the implementation plan should note it may need reconciling with whatever's actually in the tree by the time Sonnet executes, and flag if `gameday-reconciliation-implementation-plan.md`'s own constants table (which still describes the old, buggy "pursuit" behavior this fix superseded) should be updated to match — Alex has an open question about that from the other session and hasn't decided who does it yet.

**Mission constraint, unchanged:** the underlying Discord league game has no physical component — "roll pitch/swing numbers, look up the result." Everything visual is a layer invented on top of an already-decided result. **The result must never be contradicted.** Everything below is about *how* that fixed outcome gets its physical story computed, and about closing gaps in that story — never about changing what the outcome is.

## Required reading, in this order

1. This document.
2. `gameday-reconciliation-fable-prompt.md` and `gameday-reconciliation-implementation-plan.md` (repo root) — the round-1 design and plan. Don't re-litigate Task 1's architecture decision (forward-simulate + bounded reconciler) — it's adopted and working; round 2 extends it.
3. `docs/js/app.js` — current line numbers for everything cited below are accurate as of this writing; re-check with a quick grep before relying on exact numbers, since round 1's stages shifted a lot of code.
4. `import_BRC.csv` and `brc_editor.html`/`brc_editor.py` (whichever currently owns the editor UI — check both) — the `ThrowOrder`/`ThrowOrder_<POS>` column family that Task 8 below extends.

## Verified facts — round 1's actual implementation state (probed, not assumed)

1. **Round 1 shipped essentially stage-for-stage**, one commit per plan stage: primitive (`arrivalTimeS`/`legDurationsMs`/`fielderProfile`/`runnerProfile`/`throwProfile`, ~app.js:4285–4363), per-runner speed (`runnerSpd`, 2722), the reconciler + `MARGIN_POLICY`/`targetMarginMs` (~3192–3369, with the exact `holdRelease`→`quickRelease`→`runnerLateJump`→`stretchRunner` knob order and the bounded-`stretchRunner` console-warn the plan specified), consumer simplification (runner tokens read `plan.adjustments`, ~6111–6120), Stage 5a `firstBaseCoverage(m, flight)` (4535, `IF1B_DEPTH_SHIFT_FT` deleted), Stage 5b `ofPursuitApplies`/`ofPursuitDeficitMs`/`ofReadDelayMs` (4130–4178, `OF_SHIFT_DEG` family deleted), and matching test updates in `ball_flight_test.py`.
2. **One known deviation from the plan, still true today:**
   - `infieldSingleThrowHtml` (5661, exported 5903, called 6674) was supposed to be deleted and folded into a `contestedSafe` reconciler event. It's still a separate function, though it now appears to route through the shared margin machinery rather than being a fully independent backward-solve.
   - (The plan's Stage 2 archive-rebuild concern is resolved, not open — see fact 2a below. The rebuild that happened in `4cf87e7` looked at first glance like a different rebuild for a different purpose, but the `spd` backfill did in fact land for historical seasons.)
2a. **Historical s01–s12 archives do carry real per-player speed data.** Confirmed directly: `docs/data/s01/key_moments.json` has varying `"batter_spd":N` values (spot-checked: 1, 1, 2, 1, 1, ...) — not a constant/missing field. `spdPaceScale`/`runnerSpd`/`fielderSpd` are live on historical replays, not falling back to league average. Round 1's Stage 2 archive requirement is fully satisfied; no follow-up needed here.
3. **Two post-round-1 commits already extend the reconciler pattern and are directly relevant to Task 8 (cutoffs) below:**
   - `b0534c3` "reconcile explicit ThrowOrder on hits" — closed a gap where throws drawn on **hit** rows (not just outs) weren't reconciled against real runner arrivals; added `runnerForSafeTarget`/`safeRunnerArrivalMs` so a `contestedSafe`/decorative throw on a hit is honestly timed against whichever runner (if any) is actually advancing to that base. **This is the established precedent for "explicit CSV-driven throw target, reconciled against real runner state" — the cutoff design in Task 8 should extend this pattern, not invent a new one.**
   - `cfc9feb` "brc_editor: support ThrowOrder_LF/CF/RF alongside ThrowOrder_OF" — per-position outfield throw-order columns already exist in the editor and in `throwOrderCandidateKeys`/`outThrowTargets`.
   - `71f88ef` "decorative/safe throws drop the dashed line, keep the ball" — presentation-only, no architecture relevance.
4. **`THROW_SPEED_MPH = 90` (app.js:3121) is still a single flat global constant** — every throw, every position, every catcher, travels at the same speed. No per-position or per-player throwing-velocity data exists anywhere (`database.py:324`'s full attribute list is `con, eye, pwr, spd` for batters and `mov, cmd, vel, awr` for pitchers — no arm rating for any position). This was round 1's fact 0.2 and remains true.
5. **The current per-SPD-point runner speed table**, computed from `spdPaceScale(spd) = 1 + (spd-3)*0.12` (app.js:2698–2702) applied to `RUNNER_SPRINT_FT_PER_S=27` (1640):

   | spd | ft/s |
   |---|---|
   | 1 | 20.52 |
   | 2 | 23.76 |
   | 3 | 27.00 |
   | 4 | 30.24 |
   | 5 | 33.48 |

   Spread: fastest is **63.2% faster** than slowest. This is the same `spdPaceScale` used for fielder pace — one universal per-league scaling factor, not runner-specific. `runnerSpd(m, who)` (2722) is wired and reads `m.batter_spd` / `m.runners_on_base[who][2]`, null-safe to league average. **This table is unaffected by fact 26 below** — it describes steady-state top speed, and fact 26's change is purely to acceleration (how fast a runner ramps up to that speed), not the speed itself. Task 11's retune (additive ft/s-per-point model) still applies exactly as designed, now on top of a real, already-landed acceleration fix rather than the old shared-with-fielders constant.
6. **`EYE` has zero client-side (`app.js`) consumption today, but it is NOT an unrelated semantic overload — it's already the league's own established stat for catcher steal-defense.** No match for `eye`/`EYE` anywhere in `app.js`; that part of the earlier finding stands. But `batter_optimizer.py` (the league's own analytics tool, not this animation engine) already defines and uses exactly this meaning: `defense = (p["awr"] + c["eye"]) / 2.0` (batter_optimizer.py:414), labeled directly as "Team Steal Defense: Pitcher AWR + Catcher EYE" / "Combined defense = (pitcher AWR + catcher EYE) / 2" (lines 656–657, 777). So wiring catcher `EYE` into steal-throw speed isn't repurposing a batting attribute for something foreign to it — it's implementing the animation to agree with a defensive meaning this codebase's own tooling has already assigned to `EYE` for exactly this scenario. Both `EYE` and `AWR` are 1–5 rated, same scale as `SPD`/`CON`/`PWR` (confirmed via `pages/2_Scouting.py:769,814`'s `st.number_input(min_value=1, max_value=5, ...)` and `batter_optimizer.py:753`'s "CON/EYE/PWR/SPD each 1-5, sum=12").
7. **`AWR` (pitcher awareness) never leaves `database.py`'s query into any client-facing JSON field** — no match anywhere in `app.js` or `key_moments_build.py`'s emitted moment shape. Wiring it into `STEAL_LEADOFF_FT` (5728, flat `12`ft today) needs a **data-pipeline change** (a new emitted field, alongside the existing `defense`/`batter_spd`/`runners_on_base` pattern), not just a JS change. Per fact 6, `AWR` paired with catcher `EYE` is already the league's own established "combined steal defense" formula (`batter_optimizer.py:414`) — good independent grounding for treating `AWR` as the right pitcher-side signal for steal mechanics, even though it still needs new plumbing to reach the client.
8. **The current `ThrowOrder` grammar is a closed, digit-only alphabet**: `THROW_ORDER_DIGIT_TO_BASE = {"1":"1B","2":"2B","3":"3B","4":"HOME"}` (4927), and `parseThrowOrder` (4928) strips every character that isn't one of those four digits as a separator. This collides with any notation that reuses digits 1–4 for a different meaning (e.g. a `C44`-style cutoff code, Alex's original suggestion) — but Alex has since proposed a cleaner, disjoint alternative (see Task 8 below) that redefines the alphabet entirely rather than trying to extend the collision-prone digit-only one: **letters `h`/`f`/`s`/`t` for home/first/second/third base, and digits `1`–`9` for the standard MLB scorekeeping fielding-position numbers** (1=P, 2=C, 3=1B, 4=2B, 5=3B, 6=SS, 7=LF, 8=CF, 9=RF) **when throwing to a specific fielder rather than a base.** Under this scheme every leg of a throw chain is exactly one character — a base letter or a position digit — so an arbitrary-length chain (single throw, one cutoff then a base, or multiple cutoffs) never needs more than one character per leg, unlike the `C44` idea which needed two per cutoff leg. This is a **breaking format change**, not an additive one: existing `ThrowOrder` data uses digits 1–4 to mean bases (`THROW_ORDER_DIGIT_TO_BASE`); the new scheme reassigns those same digits to mean fielding positions, so any existing CSV data using the old convention needs a real migration, not a side-by-side fallback that tries to guess which convention a given digit sequence intends.
9. **No cutoff coordinate data exists anywhere.** `FIELDER_ANCHORS_FT` holds each position's normal fielding depth, not a distinct cutoff spot; a cutoff-man mechanic needs real coordinates from Alex, not something derivable from existing anchors.
10. **No "dive" mechanic and no wall-bounce/ricochet physics exist anywhere** (zero matches for "dive" in app.js; there's no collision/bounce geometry tied to a wall standing in foul ground at all; the fair-territory fence wall (`fenceWallPathD`, 2012) is a drawn visual shape, not a collision surface). **Important correction to how Task 7 was originally scoped: this is not about a foul ball.** The play in question is a live, fair, in-play batted ball whose flight or roll carries it into contact with a wall/structure that happens to sit in foul territory (e.g. a side wall down the line) — the ball stays live throughout, no foul is called, and fielders must pursue the live ricochet exactly as they would any other live ball. Both the missing dive mechanic and the missing wall-bounce mechanic are greenfield, not tweaks.
11. **The outfield pursuit race already targets the ball's final post-rollout rest point** (`fieldedPoint(flight)`, 3533–3536 — explicitly *beyond* the landing spot, after ground/roll), not the first-landing spot. This matters directly for feedback item 7 below: the premise that fielders are racing to "where the ball first lands" is **not** what's happening today. **Alex has separately confirmed the model must not require the fielder to reach the ball immediately as it comes to rest — a pause/gap between the ball stopping and the fielder's actual arrival is expected and fine, not a defect to eliminate.** This directly supports Task 3's fix (letting a throw's schedule wait for the fielder's real, possibly later, arrival) — the goal is never "force the fielder to coincide with the ball at rest," only "never schedule the throw before the fielder that's supposed to be carrying it has actually gotten there." Make this explicit in the plan so neither Task 3 nor Task 4 accidentally over-corrects into eliminating a pause that's supposed to exist. **Superseded editorial note (kept for history, no longer the operative guidance): this fact previously speculated the "looks too fast" fix was probably the missing dive-and-miss motion, not a slower race — fact 26 below shows the race itself really was running too fast (a genuine, now-fixed bug, not just a perception issue), so re-assess how much of the complaint remains before assuming Task 4's dive-and-miss motion is still the primary fix needed.**
26. **A separate, real, already-fixed bug (landed in a concurrent session, verified directly against the working tree — not yet committed): the OF pursuit race was silently running on the wrong motion profile, using full-sprint-grade acceleration for a task that should ramp up much more gently.** `ofPursuitDeficitMs` (4163) previously computed `honestArrivalMs` via `fielderLegDurationsMs(m, flight.fielder, [{distFt}])[0]` plus a separately bolted-on `OUTFIELDER_REACT_MS` — i.e. the generic **"run"** kind's numbers (`RUNNER_SPRINT_FT_PER_S=27` top speed, the old shared `FIELDER_ACCEL_FT_S2=25` acceleration), even though `fielderProfile`'s own `"pursuit"` kind branch already existed as a named, intended-but-unused code path. This is now fixed: `ofPursuitDeficitMs` calls `fielderProfile(m, flight.fielder, "pursuit")` directly (confirmed at 4170, diffed and read personally, brace-balanced), which now carries its own real, Statcast-fit constants — `OF_PURSUIT_ACCEL_FT_S2=10.2` and `OF_PURSUIT_TOP_SPEED_FT_PER_S=26.4` (4294–4295), fit against real 2026 outfielder-jump data (n=90, `f_bootup_distance` — real feet gained toward the ball's eventual spot in the first 3 seconds), with the profile's `reactionS` now properly carrying `OUTFIELDER_REACT_MS/1000` as part of the same `fielderProfile` call rather than added on separately afterward. **The practical effect: the honest OF pursuit race used to accelerate a fielder chasing a fly ball at the same aggressive 25ft/s² an infielder uses on a controlled short-range charge — now it ramps at a real, much gentler 10.2ft/s², consistent with a fielder still being well short of top speed three seconds after reading a fly ball off the bat.** This is a strong, independent, data-grounded explanation for a meaningful share of Alex's "outfielders look too fast on XBH" complaint (originally raised as feedback item 7/Task 4) — treat this as already substantially addressed by this fix, and **re-assess visually how much of the original complaint remains before investing further in Task 4's dive-and-miss motion**, rather than assuming the dive-and-miss animation is still the primary lever. Separately confirmed unaffected by this same diff: infield charge (`FIELDER_CHARGE_FT_PER_S`/`FIELDER_ACCEL_FT_S2=25`, unchanged) and the OF-charging-a-single/roller race (`OF_CHARGE_FT_PER_S=24`, unchanged) — genuinely different physical actions this Statcast dataset doesn't speak to, deliberately left alone. **This diff is currently uncommitted in the working tree (`git status`/`git diff docs/js/app.js` confirms it, 45 insertions/9 deletions, otherwise untouched) — Fable should treat it as already-landed ground truth for planning purposes, but Sonnet should confirm it's still present (or has since been committed) before building on it.**
12. **Pitcher shares the identical `CHARGE_REACTION_S=0.15` (2750) with every infielder** in the charge race (2786, 4331); the only pitcher-specific gate is the discrete `PITCHER_MIDDLE_EV_MAX_MPH=80` cutoff (2926) removing him from the fielding-candidate pool entirely above that exit velocity — no continuous reaction-time penalty exists. This is exactly round 1's Stage 5c, which was explicitly deferred as "optional, do last" and never landed.
13. **1B coverage is correctly decided but not consistently rendered.** `firstBaseCoverage`/`coveringPosition`'s 1B branch (4535, 5071) already computes who should cover 1B correctly (including the PFP pitcher-covers case). But `fielderTokensHtml`'s token-movement logic only consults that decision inside the `isNewOut && isGroundOrAir` branch (4586–4642). Any throw to 1B on a play that is **not** a new out — an infield single, or any `contestedSafe`/decorative throw to 1B under the `b0534c3` hit-reconciliation mechanism — falls into the `else if (flight.fielder)` branch (4644–4661), which moves only the charging fielder and leaves 1B's own token sitting in the idle/drift pool (4664–4669). The ball animates flying to the bag; the fielder meant to catch it never visibly gets there.
14. **The grounder-out throw path already correctly waits for the fielder** (`fieldedMs(flight) + THROW_DELAY_MS`, 5414, and the same in `infieldSingleThrowHtml`, 5667) — this constraint is real and enforced for grounders. The actual gap is narrower than "throws never wait": `fieldedMs` (3397) is pure ball-flight/roll physics with **zero knowledge of the fielder token's own animated position**. The OF honest-pursuit fix (fact 11 above) only *adds delay* when a fielder would otherwise arrive too early; it does nothing when honest pursuit is naturally *slower* than the ball's own roll-out time (the common case on a real double/triple) — nothing re-syncs the throw schedule to the fielder's real, slower arrival in that direction. That's the live version of "the ball is shown as thrown before the fielder gets there," and it's specific to outfield hits, not grounders.
15. **No collision/ordering logic exists between two simultaneously-advancing runner tokens, and this is a live risk today, not a hypothetical one.** `deriveRunnerMoves` (1664) computes each runner's `{from, to, scored}` independently with no awareness of other movers on the same play. Per-runner `spd` (fact 5) is already wired and already live in production and on historical replays (fact 2a) — so any multi-runner advance with a slow lead runner and a fast trailing runner has nothing today preventing the trailing token from visually overtaking the leader. Treat this as an active gap to close, not a preventive measure for a future change.
16. **The likely cause of the HR "fence shadow"**: the ball's own ground-projected flight shadow (`SHADOW_RX/RY`, `shadowScaleAt`, ~3691–3694; the `.ball-shadow` element/keyframes, ~3800/3900) is a *separate* animated path from the ball marker itself. The ball marker gets truncated at the fence (`fenceTruncatedSamples`, 3554, cut at fence+15ft, 3555) on a home run, but it's unconfirmed whether `shadowPts`/`shadowStops` (3704–3734) are built from that same truncated sample list or from the full untruncated flight series — if the latter, the shadow keeps animating along the ground past/through the fence wall after the ball marker has already been cut off, which reads as a shadow "on" the fence. Verify this by reading `shadowPts`/`shadowStops`'s source list before writing the fix — it's likely a small, contained change, not a design task.
17. **"Throws must not beat runners advancing to that base on hits, with a margin, and this correctly does NOT apply when nobody's actually advancing to the thrown-to base" — this is already implemented, confirmed by reading the mechanism directly, not inferred.** `runnerForSafeTarget(m, moves, targetBase)` (5324–5335) finds the specific runner (if any) whose move actually terminates at the throw's target base; if none does, it returns `null` (5330, no match falls through). `safeRunnerArrivalMs` (5343) returns `null` when `runnerForSafeTarget` finds nobody (5345). In `throwSchedule`, the final leg's reconciliation block only runs `if (finalRunnerArrival != null)` (5468) — so a decorative/cutoff-style throw to a base nobody is advancing to is correctly left unreconciled against any runner (no false margin requirement invented), exactly matching the "not all throws on hits are to bases being advanced to" nuance from the original feedback. When a runner *is* found, the throw reconciles as `contestedSafe` (throw must honestly lose, with the `MARGIN_POLICY` margin) against that runner's real arrival (5469–5472). **This satisfies the feedback item as stated for the single-runner case.** What's *not* independently confirmed: correctness on a play where **multiple** runners are advancing simultaneously and the throw target is only relevant to one of them — `runnerForSafeTarget`'s `moves.forEach` (5326) takes the *first* move (in array order) matching the target base, not an explicitly identified "the" advancing runner; worth a Task 1 spot-check on a multi-runner hit rather than assuming array order always matches intent.
18. **The glove/ball-fielding moment is a single instant, not two.** The fielder token and ball coincide at one computed `fieldedMs`/`fieldedPoint` (`resolveGrounderInterception`/`chargeInIntercept`, ~2775–2900); the glove icon (`FIELDER_GLOVE_USE`, 4056) is static markup riding the fielder's own token with no separate "glove gets ahead of the rolling ball" sub-beat. Note: the specific bug of a *throw* outracing the glove that's supposedly carrying the ball was already fixed in round 1 (comment at 5429–5434 — relay-leg draws now use the same accelerating leg-duration math as the glove token) — what's still missing is purely the visual beat of the glove arriving in front of the ball before the numeric fielding instant, for grounders specifically.
19. **A confirmed, likely-primary cause of "the throw's velocity looks like it's changing mid-air" on hit throws: the thrown ball's CSS animation uses non-linear (`ease`) timing, not constant velocity.** `.throw-clip-rect` (`docs/css/style.css:1009–1011`) and `.throw-ball` (1021–1023) both animate with `ease`, not `linear` — CSS's default ease curve decelerates sharply approaching its end, so a throw that is, in the underlying JS timing model, moving at one constant honest mph (`throwDrawMsForFt`, flat distance/`THROW_SPEED_MPH`) visibly *slows down* as it nears the base. This is not a description of the margin math being wrong; it's a real, separate rendering defect, and it's the exact bug class already found and fixed once before in this same codebase for a different token: `.fielder`'s own CSS comment (960–966) explicitly documents switching fielder-run animation from `ease` to `linear` because "was reading as decelerating into the catch... a fielder closing on a ball doesn't ease off approaching it" — the same physical argument applies to a thrown ball, which was never fixed to match. The throw's actual scheduled arrival time (`schedule.endMs`, driven by `reconcileThrowSchedule`) is unaffected by this — the CSS easing only changes how the *approach* looks, not when it numerically lands — but a ball that visibly decelerates right before reaching the base reads as "barely making it," independent of and probably compounding whatever the real computed margin is. **Investigate and very likely fix this before touching `MARGIN_POLICY.contestedSafe`'s values at all** — it may resolve a large fraction of the "too close" complaint on its own, and widening margins on top of an un-fixed easing bug risks overcorrecting for a rendering defect rather than a timing one.
20. **On hit plays, every `contestedSafe` throw — both `infieldSingleThrowHtml` (5661, `reconcileThrowSchedule(..., "contestedSafe", ...)` at 5671) and the general `b0534c3` hit-reconciliation path in `throwSchedule` (5469–5472) — closes a too-early honest arrival through exactly one knob: `holdChainTo`/`holdRelease`** (3253–3261, delaying the whole throw's release so the *same-speed* throw lands later; see `reconcileThrowSchedule`'s `delta > 0` branch, 3294–3301). There is no throw-speed knob today at all — `THROW_SPEED_MPH` (3121) is the single flat value baked into every throw's `drawMs` before the reconciler ever runs, so "throw speed" has never been an adjustable margin lever; only *when* the ball leaves the fielder's hand is. This confirms the literal ask: today, the only way to create more margin on a too-early hit throw is to hold the release longer — Task 9 below is what actually introduces throw-speed as a second, legitimate lever, and per fact 19, the CSS fix should land and be re-evaluated first since the perceived tightness may not require touching speed or margin at all.

21. **Idle (non-involved) fielder movement exists but is exponentially capped short-range, so on most plays it's invisible — this is almost certainly the "excluded" behavior Alex is noticing, not an actual removal.** `idleDriftLeg` (4105–4120), wired into `fielderTokensHtml`'s idle branch (4664–4668) for every fielder not already moving as part of the play (`!moving[pos]`), computes a small lean toward `fieldedPoint(flight)` (the ball's final rest spot) bounded by `IDLE_DRIFT_MAX_FT=10` decayed exponentially over `IDLE_DRIFT_DECAY_FT=40` (4093–4094) — at 40ft away the cap is already down to ~3.7ft, at 80ft ~1.35ft, and past ~110ft the cap falls below `IDLE_DRIFT_MIN_FT=0.5` (4095) and `idleDriftLeg` returns `null` (4112), rendering that fielder as a fully static `idleFielderTokenHtml` (4063–4068) with zero motion. Given real distances on a diamond (infield diagonal ~127ft, outfield depths 300ft+), almost any fielder more than roughly a third of the infield diagonal away from where the ball ends up gets exactly zero drift today — in practice, most of the defense on most plays. Catcher and pitcher are separately hard-excluded regardless of distance (`IDLE_DRIFT_EXCLUDED_POSITIONS`, 4096; reasoning at 4070–4077 — neither is in a fielding stance to lean anywhere). This is deliberate, already-tuned behavior, not a bug or regression — the code comment (4079–4092) documents it was refined once already, from an original flat-10ft lean that (per an earlier Alex report cited in the comment itself) "read as too uniform/sudden," to this proximity-decayed + real-accel-time-capped version. Any change here should re-tune the existing mechanism, not replace it.

22. **2B coverage is decided by spray angle, not by which outfield position threw the ball — a real, confirmed gap; 3B is already correct.** `coveringPosition`'s 3B branch (app.js:5106) already always resolves to `"3B"` for any OF throw (with one bunt-specific exception unrelated to this) — exactly matching "3B covers 3B for throws from any OF," no fix needed. Its 2B branch (5112) is `angle < 45 ? "2B" : "SS"` — keyed on the batted ball's spray angle; `fielderPos` (which position is actually making the throw) **is not read in this branch at all**. Alex wants 2B coverage decided by the throwing position instead: SS covers on throws from RF or CF, 2B covers on throws from LF. Small, contained fix — replace the angle check with a `fielderPos`-keyed lookup.
23. **The token-movement gap described in fact 13 (1B not moved on non-out plays) is already base-agnostic in both directions — good news, no per-base special-casing needed anywhere.** The `isNewOut && isGroundOrAir` chain-mover branch (4622–4642) is fully generic already: it builds `relayBases` from `outThrowTargets` and calls `coveringPosition(base, ...)` per relay base with zero base-specific logic, so once fact 22's 2B fix lands, real-out plays throwing to 2B/3B will already animate the correct covering fielder with no separate work. The gap is entirely in the **non-out** `else if (flight.fielder)` solo-mover branch (~4658–4661), which moves only the charging fielder and never a covering fielder — and this failure mode is identical for 1B, 2B, and 3B today (nothing about it is 1B-specific). **One fix, generalized across all bases, closes fact 13's gap for all three** — Task 2 below should be scoped that way explicitly, not as a 1B-only fix.
24. **A confirmed, precisely-located cause of "the glove isn't moving to the fielded ball fast enough" — the same deadline-compression fix already made for fly outs/pop-ups was never extended to grounder fielding.** `movingFielderTokenHtml`'s deadline-compression mechanism (4447–4456, comment: "only ever shortens, never extends") is real, generic, and already reusable — but it is **only ever invoked at one call site**, `isAir && e.base === null` (4677), exclusively for the fielder directly catching a fly ball/pop-up on a real out. It is **never passed** for (a) grounder fielding inside the same `isNewOut` chain (the ball-touching fielder's first leg, when the archetype is a ground archetype — `isAir` is false so `deadlineMs` stays `null`), or (b) the non-out solo-mover branch (~4658–4661) for any archetype, grounders included (no `deadlineMs` argument passed at all). Concretely: on a grounder, if the real accelerating-run duration (`fielderLegDurationsMs`) happens to exceed `fieldedMs`, nothing compresses it — the token keeps visibly running past the moment the game logic already says the ball was fielded, which reads exactly as "the glove isn't getting there fast enough." The fix reuses the existing generic compression code, no new mechanism needed — just wire `deadlineMs = startDelay + fieldedMs(flight)` into the ball-touching fielder's leg in both the chain-mover and solo-mover paths, for ground archetypes too.
25. **Force-out-without-an-actual-out is already correctly classified — no gap, confirmed by reading the exact logic.** `sequentialThrowSchedule` sets each leg's `out: i < realCount` (5291), where `realCount = realOutThrowCount(m, flight)` (5135–5138) is computed strictly from the real predetermined result — `Math.max(0, (m.outs_after||0) - (m.outs_before||0))` minus the batter's own air-catch-out — never from any assumption about which bases are "normally" force plays. On Alex's exact example (a clean single to CF, OF throw to 2B, zero real outs on the play), `realCount` is 0, so the 2B leg's `.out` is `false`, routing `throwSchedule`'s reconciliation (5461–5466) through `runnerForSafeTarget`/`safeRunnerArrivalMs` and the `contestedSafe` policy (throw must honestly lose to the real advancing runner) — never `forceOut`. This is already handled correctly by the existing `b0534c3` mechanism. No design task needed; add it as an explicit regression-test case in `ball_flight_test.py` instead (Task 1).

## Tasks

### Task 1 — Verification and gap-closing on the round-1 refactor

Using fact 2 above: decide whether `infieldSingleThrowHtml` should now actually be deleted and folded into the `contestedSafe` reconciler path (the plan's original intent), or whether its survival is now load-bearing for something the general path doesn't handle (check what it does that the general throw schedule + `b0534c3`'s hit-reconciliation doesn't). Recommend one and design it if folding is still right.

(The archive-`spd` question from round 1 is resolved — fact 2a — no task needed.)

Using fact 17 above: spot-check `runnerForSafeTarget`'s "first matching move wins" behavior (5326) on a real multi-runner hit (e.g. a single with runners on 1st and 2nd both advancing, and a throw target that's genuinely relevant to only the trailing runner) to confirm array order actually reflects the intended runner in every case, not just the common single-runner case. If it can misidentify the runner on some play shape, that's a small, contained fix within the existing function — not a new mechanism.

Using fact 25 above: add an explicit regression-test case to `ball_flight_test.py` for a force-situation base with zero real outs on the play (e.g. a CF single with an OF throw to 2B and no recorded out) asserting the leg reconciles as `contestedSafe`, not `forceOut` — this is already correct behavior, but Alex raised it as a specific worry, so it's worth a permanent test rather than only today's static-read confirmation.

### Task 2 — Fix throw-receiver coverage: token movement on non-out plays (all bases), and 2B's coverage-by-thrower logic (facts 13, 22, 23)

Two related, separably-landable pieces:

1. **Token movement on non-out plays, generalized to every base — not a 1B-specific fix.** Per fact 23, the `isNewOut` chain-mover branch already generalizes correctly across bases; the actual gap is confined to the non-out `else if (flight.fielder)` solo-mover branch (~4658–4661), which moves only the charging fielder regardless of which base a `coveringPosition`-derived throw target is. Extend the token-movement logic so any throw target derived from `outThrowTargets`/`coveringPosition` — out or not, and regardless of base — gets its receiving fielder's token moved there. This needs to interoperate with the existing solo-mover branch without breaking the charging fielder's own motion on the same play (two fielders may need coordinated motion: the one fielding the ball, and the one covering the base it's thrown to). `firstBaseCoverage`/`coveringPosition`'s decisions should be the single source of truth for which fielder moves, consistent with round 1's "one shared mechanism" principle — design this generically across 1B/2B/3B, since fact 23 confirms the failure mode is identical for all three.
2. **Fix `coveringPosition`'s 2B branch to key off the throwing position, not spray angle.** Per fact 22: 3B (5106) is already correct as-is (always `"3B"` for an OF throw, one unrelated bunt exception) — no change needed there. 2B (5112) currently reads `angle < 45 ? "2B" : "SS"`, ignoring `fielderPos` entirely; replace with a `fielderPos`-keyed rule: RF or CF throwing → SS covers; LF throwing → 2B covers himself. Decide what governs non-OF throws to 2B (e.g. an infield relay) — presumably unaffected, confirm the existing angle-based fallback (or an equivalent) still applies there rather than being fully replaced.

### Task 3 — Resync outfield-hit throw scheduling to the fielder's real arrival (facts 14, 26)

**Fact 26 makes this task more, not less, important.** The OF pursuit race just got genuinely slower and more honest (10.2ft/s² real acceleration instead of the old, buggy 25ft/s²), which means honest pursuit now lags behind the ball's roll-out time on *more* plays than before the fix — this task's fix is the direct counterpart needed to keep the throw schedule honest against that now-more-realistic (and now-slower) pursuit. Design the missing direction of Stage 5b's fix: when honest OF pursuit is *slower* than the ball's own roll-out time (fielder hasn't visually reached `fieldedPoint` yet), the throw schedule must not start until the fielder token's own real arrival, not `fieldedMs` alone. This is the natural counterpart to `ofReadDelayMs` (which only handles the fielder being *too fast*) — likely the cleanest fix is for `fieldedMs`/the throw-scheduling entry point to take `max(ballPhysicsFieldedMs, fielderTokenArrivalMs)` for outfield plays specifically (infield grounders already behave correctly and shouldn't change). Confirm this doesn't fight the existing reconciler's `holdRelease` knob — it may turn out `holdRelease` already covers this if fed the right honest-arrival input, in which case the fix is "feed the reconciler the fielder's real token arrival," not a new mechanism.

### Task 4 — Dive-and-miss motion for XBH hit at/near a fielder (facts 10, 11, 26)

**Re-assess scope against fact 26 before designing this in full.** A real, separate bug in the OF pursuit race's own speed/acceleration (fact 26 — it was silently using full-sprint-grade 25ft/s² acceleration instead of the real, much gentler 10.2ft/s² a fielder actually shows chasing a fly ball) has just been fixed in a concurrent session. Since that bug alone could account for a meaningful share of "fielders look too fast on XBH," **visually re-test the current pursuit behavior with fact 26's fix in place before committing to the full two-leg dive-and-miss design below** — it's possible the honest pursuit now already looks convincing on its own for many plays, and the dive-and-miss motion should be scoped to the cases that still look wrong after that, not designed as if fact 26 never happened.

Where a dive-and-miss motion is still warranted: design a genuinely new two-leg fielder motion for outfield hits that go for extra bases at/near a fielder (the current single straight-line leg to `fieldedPoint`, 4650–4660): leg 1 converges on the ball's *first-landing* point (a believable diving-effort route, timed to arrive at or just after the ball lands — i.e., missing the dive, not beating it there); leg 2 chases the bounce from landing point to `fieldedPoint` (the honest post-rollout rest point pursuit already computes, per fact 11, now on fact 26's corrected pursuit profile). What's missing is a route shape that reads as an effort-and-miss instead of an unnaturally efficient beeline — not necessarily a speed problem anymore, now that fact 26 has landed. Reuse the existing multi-leg fielder-motion machinery already used elsewhere (e.g. the unassisted relay-chain case, ~4624–4631) rather than inventing new leg-sequencing code. Scope this to plays where a dive is actually plausible (a ball landing within realistic diving range of the fielder's position) — don't apply it to routine backhand plays or balls that land far from anyone.

### Task 5 — Extend the existing fly-out/pop-up glove-deadline fix to grounder fielding (facts 18, 24)

**Precisely located by direct investigation — not a new mechanism, a missing wiring.** Per fact 24, the deadline-compression fix already shipped and working for fly outs/pop-ups (`movingFielderTokenHtml`'s `deadlineMs` compression, 4447–4456 — "only ever shortens, never extends") is only ever invoked at one call site: `isAir && e.base === null` (4677), exclusively for the fielder catching a ball in the air on a real out. It is never passed for grounder fielding — neither the ball-touching fielder's leg inside the `isNewOut` chain-mover branch when the archetype is a ground archetype (`isAir` is false there, so `deadlineMs` stays `null`), nor the non-out solo-mover branch (~4658–4661) for any archetype including grounders. This is exactly Alex's report: "the glove... isn't moving to the fielded ball fast enough... the same issue we fixed for fly outs and popouts." Fix: wire `deadlineMs = startDelay + fieldedMs(flight)` into the ball-touching fielder's leg in both the chain-mover and solo-mover paths for ground archetypes too, reusing the existing generic compression code as-is — no new mechanism, no new sub-beat, just two additional call sites. This supersedes the original, vaguer framing of this task (a hypothetical distinct "glove gets ahead of the ball" visual beat) — the real fix is simpler: make the glove token's own arrival honor the same fielding deadline the ball's timing model already computes, the same way it already does for fly balls.

### Task 6 — Pitcher charge-in reaction delay (fact 12)

Land round 1's deferred Stage 5c: give the pitcher a longer, physically justified reaction delay in the charge race (finishing a pitching motion is a real, non-zero cost — the pitcher "isn't fully ready to run and field the ball" the instant the ball is put in play, per Alex) so that balls in the zone between the mound and 3B correctly go to a charging third baseman more often instead of being fielded short by the pitcher. Alex's own framing of the magnitude: **"a split second more" than the other infielders' shared `CHARGE_REACTION_S=0.15`** — treat that as guidance toward a modest bump (order of a few hundred ms on top of 0.15s, not a full extra second or more), not a target to hit exactly; the actual value should still be validated against real recorded plays in the pitcher/3B contested zone, not picked from the phrase alone. Decide between a flat pitcher-specific `CHARGE_REACTION_S` override vs. the continuous reaction-window model round 1 sketched (`gameday-reconciliation-implementation-plan.md` §5c) — either is acceptable, but tune against Alex's stated complaint (pitcher fielding balls that should clearly be 3B's play) rather than picking a number blind; a probe script sweeping real recorded plays in the pitcher/3B contested zone is the right way to validate the choice before finalizing.

### Task 7 — Live-ball ricochet off a wall standing in foul territory (fact 10)

**Not a foul ball — read fact 10's correction first.** The scenario is a live, fair, in-play batted ball whose flight or roll carries it into a wall/structure that happens to sit in foul ground (e.g. a side wall down the line). The ball remains live the entire time; no foul is called; play continues exactly as it would after any other bounce. Design real bounce physics for the ball striking that wall: an angle-of-incidence-based deflection (doesn't need to be exact physics — a simple reflection off the wall's normal with some energy loss is plausible and consistent with the project's existing "plausible, not exact" bar for ball flight), continued rolling/bouncing after the ricochet, and fielder pursuit of the resulting ball position using the same honest-pursuit machinery as any other live ball. This is genuinely new geometry and physics, not a parameter tweak — treat it with the same rigor as round 1's grounder-interception design. Scope: side walls standing in foul territory only (fair-territory outfield fence behavior is unchanged and out of scope, same boundary as round 1).

### Task 8 — Cutoff throws: `ThrowOrder` notation + mechanic (facts 3, 8, 9)

The largest new-mechanic task. **Alex's current proposal (supersedes the original `C44` idea, per fact 8): redefine the `ThrowOrder` alphabet entirely rather than layering a cutoff prefix onto the existing digit-to-base convention.**
- Letters `h`/`f`/`s`/`t` mean home/first/second/third **base**.
- Digits `1`–`9` mean the standard MLB scorekeeping fielding-**position** numbers when throwing to a specific fielder rather than a base: 1=P, 2=C, 3=1B, 4=2B, 5=3B, 6=SS, 7=LF, 8=CF, 9=RF.
- Every leg of a throw chain is exactly **one character**, so a value like `RF throws to the cutoff shortstop, who throws home` is `6h` (position 6 = SS, then base h = home) instead of the two-character-per-leg `C44` shape originally proposed — and a chain with multiple cutoffs (e.g. through two relay fielders before reaching a base) is just as many single characters in sequence, with no special-casing needed for "how many legs does this chain have."
- This is a clean, elegant scheme (reuses baseball's own standard position numbering) but it is a **breaking change**, not additive: today's digits 1–4 already mean bases (`THROW_ORDER_DIGIT_TO_BASE`, fact 8); under the new scheme the same digits mean *positions*. Any existing `ThrowOrder`/`ThrowOrder_<POS>` data written under the old convention must be migrated, not auto-detected — the plan needs an explicit migration step (a one-time conversion pass over `import_BRC.csv`'s existing `ThrowOrder*` columns), not a runtime dual-format parser trying to guess intent from a sequence of digits alone.

Design, at minimum:
1. **The new grammar itself.** Each character in a `ThrowOrder` value is one leg target: `h`/`f`/`s`/`t` → base, `1`–`9` → fielding position. Parsed left to right into an ordered leg list. `parseThrowOrder` needs a real rewrite around this alphabet (not a regex tweak on the old one) — decide explicitly whether the parser should reject/warn on a malformed or ambiguous value (e.g. a stray character outside both alphabets) rather than silently stripping it the way today's separator-stripping behavior does, since silent stripping under the old convention was low-risk (extra separators) but would be much riskier here (a stray digit is a plausible-looking wrong leg, not obvious noise).
2. **Cutoff coordinate data.** No cutoff spot exists anywhere in the codebase today (fact 9) — this needs new data Alex will supply (per-position, possibly per-situation, cutoff coordinates). Propose a concrete format (new `import_BRC.csv`/editor columns, analogous to how `ThrowOrder_LF/CF/RF` already split from `ThrowOrder_OF` per `cfc9feb`) and flag exactly what you need from Alex to proceed — don't invent placeholder coordinates.
3. **A fielder-position waypoint in the throw chain, supporting an arbitrary number of them per play.** `outThrowTargets`/`coveringPosition` currently assume every chain entry is a *base*; a cutoff leg's intermediate target is a *fielding position at a cutoff coordinate*, not a base, and per point 1 above a single play's chain may contain more than one such waypoint before the final base. Design how this waypoint type flows through the existing relay/stagger/reconciler machinery (the `b0534c3` "reconcile explicit ThrowOrder on hits" mechanism, fact 3, is the closest existing precedent — explicit CSV-driven throw target, reconciled against real runner state — extend that pattern rather than building a parallel one).
4. **Explicitly confirm the scope boundary from round 1 still holds**: this is a **pre-declared** cutoff (the CSV says the ball goes fielder→cutoff→base before the play starts), not a dynamic mid-flight redirect decision made by the animation engine. Round 1 explicitly scoped true dynamic cutoff-man redirection out (`gameday-reconciliation-fable-prompt.md:55`); Alex's notation proposal is data-declared, which is a different (and much smaller) problem — say so explicitly in the plan so Sonnet doesn't over-build.
5. **Migration of existing `ThrowOrder*` data to the new alphabet.** Every `ThrowOrder`/`ThrowOrder_<POS>` value already written under the old digit-means-base convention needs a one-time, explicit conversion to the new letter-means-base/digit-means-position scheme (point 1's grammar) before the new parser can be trusted on real data — inventory every column this touches (`ThrowOrder`, `ThrowOrder_OF`, and the per-position splits from `cfc9feb`: `ThrowOrder_LF/CF/RF`) and write the conversion as a real, auditable step (e.g. a small one-time script Sonnet runs once against `import_BRC.csv`, not a permanent runtime shim). Confirm nothing downstream of the CSV (the editor UI, `brc_editor.py`/`.html`) still writes the old convention after the cutover, or old-format rows will keep reappearing.

### Task 9 — Fix perceived-closeness on hit throws: rendering, margin, and throw-speed variance by position (facts 4, 19, 20)

Alex's report from playtesting: throws on hit plays are arriving at bases where a runner is advancing "way too early... so close," and separately, the throw's own velocity looks like it's "almost changing mid-air." Four sub-parts, **in this order — each should be evaluated before deciding the next is still needed, not designed and landed all at once:**

1. **Fix the throw-ball CSS easing first (fact 19) — likely the primary cause of the "velocity changing mid-air" report, and possibly a large share of the "too close" report too.** `.throw-clip-rect` (style.css:1009–1011) and `.throw-ball` (1021–1023) animate with `ease`; switch both to `linear`, matching the fix already made for `.fielder` (960–967) for the identical complaint ("was reading as decelerating into the catch"). Check whether the two animations' `ease` was load-bearing for keeping the ball and the line's leading edge in sync (the comment at style.css:1017–1020 says they were deliberately matched to each other) — if so, both must move to `linear` together, not just one. This is a small, contained, high-confidence fix — land and re-assess visually before touching anything else in this task.
2. **Re-assess `MARGIN_POLICY.contestedSafe` (150–450ms, fact 20) only after step 1 lands.** If hit throws still read as too close once the ball moves at genuinely constant visual speed, that's real evidence the margin itself is too tight — the same category of fix as round 1's `TAG_THROW_MARGIN_MS` 200→400 bump (a hand-tuned legibility widening with no new physics, done for exactly this "reads too close for what it is" reason). Sample real hit plays across the diff range (near-tie and decisive) and judge whether `contestedSafe`'s current 150–450ms band needs raising, and if so to what — recommend concrete new `minMs`/`maxMs` values, not just "consider it."
3. **Author a per-position throw-speed table** from Alex's supplied averages/ranges (P 85, 80–90; C 80, 75–90; 1B 80, 70–85; 2B 80, 70–85; 3B 85, 80–90; SS 85, 80–90; LF 87, 75–95; CF 90, 75–95; RF 90, 75–95 — all mph). Since no per-player arm data exists (fact 4), this is a position-only table, not per-player — say so plainly, consistent with round 1's fact-0.2 finding. Replace flat `THROW_SPEED_MPH=90` with a per-position lookup feeding `throwProfile`.
4. **Add throw-speed variance as a reconciler knob, and make it the preferred mechanism, not a fallback.** Fact 20 confirms today's reconciler has exactly one lever for a too-early `contestedSafe`/`forceOut` throw — `holdRelease`, which only delays when the ball leaves the hand, never how fast it travels once thrown. Alex's ask, stated plainly: **solve directly for the exact constant throw speed that produces the required margin, and throw at that speed, rather than defaulting to a delayed release.** Concretely — given the required arrival time (`runnerArrival ± targetMargin`), the real throw distance, and the position's real speed floor (point 3's table), compute the single constant mph within that floor-to-ceiling range that makes the honest throw land exactly on the required time; only if the required speed would fall *below* the position's realistic floor (the throw can't be slowed down enough to create the needed margin without becoming implausible) should the reconciler fall back to `holdRelease` (or a combination of both) to make up the remainder. **Hard requirement, not a detail: a throw's speed is chosen once, at scheduling time, as a single constant mph for that throw's entire flight — never varies mid-flight.** (This is what makes the knob physically honest — a real thrown ball doesn't change speed in the air; the *choice* of which constant speed to throw at is what varies play to play. Combined with step 1's linear-timing fix, the rendered ball will now honestly move at whatever single speed was chosen.) Design this as the primary knob in the reconciler's ordered list for closing a too-early `contestedSafe` gap, with `holdRelease` as the secondary/overflow knob rather than the default, and justify the choice against goal (b)'s realism bar.

### Task 10 — Prevent baserunners from passing each other (fact 15, 5) — active gap, real per-runner speed is already live in production

Design a per-play ordering constraint among simultaneously-advancing runners: given each runner's independently-computed leg timing (already real per-`spd` timing today — fact 15, not a future change), detect when a trailing runner's position would overtake a leading runner's position at any point along the shared basepath, and resolve it with a bounded, named adjustment (in the same spirit as the existing reconciler knobs — e.g., cap the trailing runner's effective speed to never close the gap below some minimum separation, rather than letting them draw level or cross). Scope to runners on the same basepath direction on the same play; runners moving in different directions (e.g., one scoring while another advances) don't need this constraint. Reuse the existing `MARGIN_POLICY`-style "named, bounded adjustment" pattern rather than inventing an unrelated mechanism. Because this can already manifest on live and historical data today (fact 2a confirms historical archives have real varying `spd` too), treat this as a real bug to fix, not speculative hardening.

### Task 11 — Retune runner speed to a flat, additive ft/s-per-SPD-point model (fact 5)

**This is a concrete spec, not an open tuning question — Alex has specified the target table.** Runner sprint speed should map from SPD rating to ft/s as:

| spd | ft/s |
|---|---|
| 1 | 25 |
| 2 | 26 |
| 3 | 27 |
| 4 | 28 |
| 5 | 29 |

i.e. `RUNNER_SPRINT_FT_PER_S` (27, the spd-3/league-average case) plus a flat **+1 ft/s per SPD point**, not the current multiplicative `spdPaceScale` (`1 + (spd-3)*0.12`) — replace the spread from today's 63.2% (20.52–33.48 ft/s) with a much flatter ~16% spread (25–29 ft/s), consistent with real-world MLB sprint-speed variance.

Scope this change to **runners only**. `runnerProfile` (app.js:4337–4343) currently computes `topSpeedFtPerS: RUNNER_SPRINT_FT_PER_S * spdPaceScale(runnerSpd(m, who))` (4339) — replace this one line's scaling with the new additive formula (e.g. `RUNNER_SPRINT_FT_PER_S + (runnerSpd(m, who) - SPD_AVERAGE) * 1.0`, null-safe the same way `runnerSpd` already is). **Do not change `fielderProfile`** (4324–4333) — fielder charge/pursuit/pace speed keeps the existing multiplicative `spdPaceScale`, since Alex's new numbers are specifically about baserunning sprint speed, not fielding quickness; the two were only sharing a formula by construction, not by requirement. Confirm no other consumer of `spdPaceScale(runnerSpd(...))` exists outside `runnerProfile` that would also need updating (grep before assuming it's the only call site).

This also reduces (but doesn't eliminate — still needs Task 10) how often the passing-prevention knob would need to fire, since the flatter spread narrows the gap between a fast trailing runner and a slow lead runner on a shared basepath.

### Task 12 — Fence shadow fix (fact 16)

Verify fact 16's hypothesis (read whether `shadowPts`/`shadowStops`, ~3704–3734, are built from `fenceTruncatedSamples` or the full untruncated series) and, if confirmed, design the one-line-scale fix (truncate the shadow's sample list identically to the ball marker's). This is deliberately scoped small — don't expand it into a shadow-system redesign unless the probe reveals something more structural than expected.

### Task 13 — Widen idle (non-involved) fielder drift so it's visible on more plays (fact 21) — explicitly low priority

Alex's framing: "not a huge priority, but a nice touch." Keep this scoped small and don't let it compete for attention with Tasks 1–12. The existing `idleDriftLeg` mechanism (fact 21) is sound in shape — proximity-decayed lean toward the ball's landing spot, capped by real accelerating-run time available before the play resolves — but tuned so tightly (`IDLE_DRIFT_MAX_FT=10`, `IDLE_DRIFT_DECAY_FT=40`) that on most plays nearly the entire non-involved defense reads as frozen, since real infield/outfield distances mostly exceed the ~110ft range where the cap still clears `IDLE_DRIFT_MIN_FT`. Recommend a wider `IDLE_DRIFT_MAX_FT` and/or `IDLE_DRIFT_DECAY_FT` so fielders roughly an infield-diagonal away still show a small, subtle acknowledgment of the play — this must stay a "creep," not a second real fielder pursuit, consistent with the mechanism's existing design intent (it is explicitly not `chargeInIntercept`-style pursuit). Re-check against the same "too uniform/sudden" complaint the original flat-10ft version drew (per the code's own comment, 4079–4092) so a wider cap doesn't regress into that — this is a tuning pass on an existing, already-twice-refined mechanism, not new architecture.

### Task 14 — Wire catcher `EYE` (throw speed) and pitcher `AWR` (leadoff distance) into steal mechanics, per the league's own established combined-defense formula (facts 6, 7)

**No longer an open design question — Alex has confirmed the direction, and fact 6 grounds it in existing precedent.** `batter_optimizer.py` already defines `(pitcher AWR + catcher EYE) / 2` as "combined steal defense" for this exact league — the animation should implement steal mechanics consistent with that existing meaning, not invent an unrelated one. Two separate wiring tasks (they affect different mechanics, don't force them into one function):

1. **Catcher `EYE` → steal-throw speed.** Design an `eyeArmScale`-style function analogous to `spdPaceScale` (both `EYE` and `SPD` are 1–5 rated, per fact 6) that scales a catcher's steal-throw speed within the `C` position's real range from Task 9's per-position table (80mph average, 75–90mph range) — a high-`EYE` catcher throws nearer the top of that range, a low-`EYE` catcher nearer the bottom. Wire this into `stealThrowHtml`/whatever consumes `CATCHER_POP_MS`/throw-speed for steal plays. This is JS-side only — `EYE` already reaches the client (it's on the batter/player record fetched for the game), confirm the steal-play moment shape actually carries the catcher's own `EYE` value (likely via `m.defense["C"]`, the same shape `fielderSpd`/`runnerSpd` already read) rather than assuming it without checking.
2. **Pitcher `AWR` → steal leadoff distance.** Per fact 7, `AWR` does not reach the client today — this needs a real `key_moments_build.py` change (a new emitted field, alongside the existing `defense`/`batter_spd`/`runners_on_base` pattern) before any JS work is possible. Once available, replace `STEAL_LEADOFF_FT=12` (5728) with a per-play formula using the pitcher's real `AWR` — Alex's own suggested shape is `15 - awr`, but confirm the sign and scale make sense against real `AWR` value ranges (1–5, per fact 6) before committing: at `awr=5` that formula gives a 10ft lead (shorter, appropriately, for a more aware pitcher), at `awr=1` a 14ft lead — sanity-check this reads right compared to `STEAL_LEADOFF_FT`'s current flat 12ft, and adjust the formula's constants if the range feels off, rather than taking `15 - awr` as fixed.

## Open factual questions (resolve with a probe script if genuinely load-bearing, don't guess)

1. **Does `infieldSingleThrowHtml` (fact 2) do anything today that the general reconciled throw path + `b0534c3`'s hit-reconciliation genuinely can't** — i.e., is its survival load-bearing, or is it now dead weight that should be deleted per the original plan?
2. **`runnerForSafeTarget`'s "first array-order match" behavior on multi-runner plays** (fact 17, tied to Task 1's spot-check) — resolve with a probe against a real multi-runner hit before deciding whether it needs a fix.
3. **Does the client-side steal-play moment shape actually carry the catcher's own `EYE` value today** (Task 14 point 1) — confirm via `m.defense["C"]` or wherever `fielderSpd`/`runnerSpd` read their per-player numbers, don't assume it's there just because `EYE` is on the player record.
4. **Is pitcher `AWR` queryable from wherever `defense`/`batter_spd` currently come from** (Task 14 point 2, same source table/join) — scope the `key_moments_build.py` emission change as its own small pipeline task before any JS-side `STEAL_LEADOFF_FT` formula change.

## What to produce

A staged implementation plan covering, at minimum:
1. **Task 1's verification findings and disposition** (delete-or-keep `infieldSingleThrowHtml`; `runnerForSafeTarget` multi-runner spot-check outcome).
2. **Per-task design** (Tasks 2–14 above), each with concrete function/constant names and file:line targets in the style of the round-1 plan's Task 3/4/5 sections. Task 13 is explicitly low priority — keep its design proportionate (a tuning-constant change), not padded out to match the depth of the higher-priority tasks.
3. **A fix map / disposition table** for every constant or function newly touched, in the round-1 plan's style.
4. **Staging order** — group into commits the way round-1's plan did (§7), noting dependencies (e.g., Task 9's four sub-parts land and get re-assessed in the sequence given — easing fix, then margin re-check, then the position table, then the speed-variance knob, not all at once; Task 11's speed retune should land before Task 10's passing-prevention knob is tuned, since a smaller spread changes how often it fires; Task 9's per-position table should land before Task 8's cutoff mechanic if cutoff throws are meant to use position-appropriate speeds; Task 9's per-position table should also land before Task 14 point 1, since catcher `EYE` scaling reads from that same table's `C` range).
5. **Test-plan updates** for `ball_flight_test.py`, per new mechanic, including the fact-25 regression case from Task 1.
6. **Explicit open questions for Alex** — especially open factual questions 3 and 4 (whether `EYE`/`AWR` actually reach the client-side data Task 14 needs) if a probe can't resolve them cleanly.
